name: site_and_structure
description: Site & Structure — engineering course platform (student app)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.6
  video_player: ^2.9.1
  webview_flutter: ^4.8.0
  http: ^1.2.1
  image_picker: ^1.1.2
  shared_preferences: ^2.2.3
  provider: ^6.1.2
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
